# Sub-proyecto 6 — Billing Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar billing end-to-end (trial → active vía Wompi recurring + DIAN multi-provider) según spec `docs/superpowers/specs/2026-05-24-billing-design.md` (commit `fa3cb9e`).

**Architecture:** Híbrida — schema completo en Postgres (Migración 046) como source of truth, Wompi solo procesa charges puntuales (PCI offload total vía widget para 1er charge + payment_source_id recurrent), cron diario procesa renovaciones, DIAN async via job table-based, framework provider-agnostic (Alegra MVP + Manual, futuros Siigo/FacturaTech).

**Tech Stack:** Next.js 14 App Router + Supabase Postgres (RLS) + Wompi REST API v1 + Alegra REST API + Resend (email) + Vercel Cron + React (UI). TypeScript estricto. Tailwind v3 + shadcn/ui.

**Timeline estimado:** 7-10 días dev + 2-3 testing.

---

## Pre-requisitos del usuario (antes de Task 1)

1. **Cuenta sandbox Wompi** (ya confirmada disponible). User debe pasar 4 keys al iniciar:
   - `NEXT_PUBLIC_WOMPI_PUBLIC_KEY` (prefijo `pub_test_`)
   - `WOMPI_PRIVATE_KEY` (prefijo `prv_test_`)
   - `WOMPI_EVENTS_SECRET` (prefijo `test_events_`)
   - `WOMPI_INTEGRITY_SECRET` (prefijo `test_integrity_`)

2. **Cuenta sandbox Alegra** (user va a crearla):
   - Signup en https://www.alegra.com/colombia/
   - Configuración → API → genera API Token
   - User pasa `email_user` + `api_key` en Task 22 (DIAN config UI)

3. **Generar secrets locales:**
   ```bash
   # CRON_SECRET (auth para cron endpoints)
   openssl rand -hex 32
   # BILLING_CREDS_ENC_KEY (AES-256-GCM key, 32 bytes base64)
   openssl rand -base64 32
   ```

4. **Setear todas las env vars en Vercel** (production + preview + development) antes de Task 23 (deploy):
   ```
   NEXT_PUBLIC_WOMPI_PUBLIC_KEY=
   WOMPI_PRIVATE_KEY=
   WOMPI_EVENTS_SECRET=
   WOMPI_INTEGRITY_SECRET=
   CRON_SECRET=
   BILLING_CREDS_ENC_KEY=
   BILLING_ENABLED=false   # feature flag inicial; flip a true en Task 24
   ```

---

## Subagent briefing template

Cuando despaches subagents (modo subagent-driven), incluye SIEMPRE este briefing:

> **Project:** App Horarios — multi-tenant SaaS scheduling (Spanish UI, Colombia).
> **Stack:** Next.js 14 App Router + Supabase Postgres + Tailwind v3 + shadcn/ui.
> **Skills disponibles (consultar antes de modificar):**
> - `supabase:supabase` y `supabase:supabase-postgres-best-practices` (RLS, migraciones, queries)
> - `vercel:nextjs`, `vercel:next-cache-components`, `vercel:vercel-functions` (cron + Fluid)
> - `vercel:shadcn` (componentes UI)
> - `vercel:react-best-practices` (TSX)
> - `modern-web-guidance:modern-web-guidance` MANDATORIO antes de tocar UI/CSS/client-JS
> - `regen-types` skill después de migración (project ugkvuinkynvtuiutwlkd)
> - `migration-reviewer` skill SIEMPRE después de crear migración nueva
> - `schedule-algorithm-reviewer` NO aplica acá (billing no toca scheduling)
> - `superpowers:test-driven-development` para tareas con lógica (engine, dunning, crypto)
> **Model:** Usa Sonnet 4.6 o Opus 4.7 — NUNCA Haiku para trabajo de código sustantivo.
> **Conventions:** UI en español (acentos correctos), no emojis en código, lucide icons, `output:standalone` Next.js, `day_of_week 0=Sunday`, fechas `YYYY-MM-DD`.
> **Spec:** `docs/superpowers/specs/2026-05-24-billing-design.md` es la fuente de verdad.

---

## Phase 1 — Database foundation

### Task 1: Migración 046 + types regen + SQL tests

**Files:**
- Create: `supabase/migrations/046_billing_schema.sql`
- Create: `supabase/tests/billing_rls_isolation.sql`
- Create: `supabase/tests/billing_unique_constraints.sql`
- Create: `supabase/tests/billing_exempt_skip.sql`
- Create: `supabase/tests/billing_plans_seed.sql`
- Create: `supabase/tests/migration_046_schema.sql`
- Modify: `src/lib/supabase/database.types.ts` (regen)

- [ ] **Step 1: Scaffold migración con skill** `/new-migration billing_schema` → crea `046_billing_schema.sql`

- [ ] **Step 2: Pegar contenido SQL del spec sección 3** literal (BEGIN/COMMIT, 6 tablas, ALTER orgs, seed plans, UPDATE LR, RLS policies). El SQL completo está en spec; no se repite acá para evitar duplicación.

- [ ] **Step 3: Crear SQL test `migration_046_schema.sql`** verificando tablas + columnas + FK + RLS aplicada:

```sql
BEGIN;
-- Tablas existen
SELECT COUNT(*) = 6 FROM information_schema.tables
  WHERE table_schema = 'public'
    AND table_name IN ('plans','payment_methods','subscriptions','invoices','payments','billing_providers');

-- organizations tiene nuevas columnas
SELECT column_name FROM information_schema.columns
  WHERE table_name = 'organizations' AND column_name IN ('billing_exempt','current_plan_id');

-- RLS habilitado en las 6 tablas
SELECT tablename FROM pg_tables
  WHERE schemaname='public' AND tablename IN ('plans','payment_methods','subscriptions','invoices','payments','billing_providers')
    AND rowsecurity = true;

-- Plans seed presente
SELECT id, price_cop FROM plans WHERE id IN ('starter','pro','enterprise') ORDER BY display_order;

-- LR grandfather
SELECT billing_exempt, current_plan_id FROM organizations WHERE id='00000000-0000-0000-0000-000000000001';
ROLLBACK;
```

- [ ] **Step 4: Crear SQL test `billing_rls_isolation.sql`** — admin org A no ve subs/invoices/pms de org B:

```sql
BEGIN;
-- Crea 2 orgs, 2 admins, 2 subs
INSERT INTO organizations (id, name, slug) VALUES
  ('aaaaaaaa-0000-0000-0000-000000000001', 'Org A Test', 'org-a-test'),
  ('bbbbbbbb-0000-0000-0000-000000000002', 'Org B Test', 'org-b-test');
-- ... seed users via auth.users, profiles, subscriptions
-- SET LOCAL ROLE authenticated; SET LOCAL request.jwt.claims TO '{"sub":"...admin-a-id..."}';
-- SELECT * FROM subscriptions; -- debe devolver SOLO la de org A
ROLLBACK;
```
(Patrón completo basado en `supabase/tests/` existentes — el subagent debe seguir el patrón ya establecido en otros tests RLS del repo.)

- [ ] **Step 5: SQL test `billing_unique_constraints.sql`** — UNIQUE en `payments.provider_transaction_id` y `subscriptions(organization_id)`.

- [ ] **Step 6: SQL test `billing_exempt_skip.sql`** — query del cron `WHERE billing_exempt=false AND current_period_end <= now()` excluye LR.

- [ ] **Step 7: SQL test `billing_plans_seed.sql`** — 3 planes con `id IN ('starter','pro','enterprise')`, prices > 0.

- [ ] **Step 8: Invocar skill `migration-reviewer`** para auditar la migración (RLS, FK, triggers, idempotencia).

- [ ] **Step 9: CHECKPOINT** — pedir al user autorización para aplicar a Supabase Cloud (project `ugkvuinkynvtuiutwlkd`).

- [ ] **Step 10: Aplicar migración** vía Supabase MCP `apply_migration` o `execute_sql` con el contenido completo.

- [ ] **Step 11: Ejecutar los 5 SQL tests** vía Supabase MCP `execute_sql` y verificar todos PASS.

- [ ] **Step 12: Invocar skill `regen-types`** para regenerar `src/lib/supabase/database.types.ts`.

- [ ] **Step 13: Commit + push**

```bash
git add supabase/migrations/046_billing_schema.sql supabase/tests/billing_*.sql supabase/tests/migration_046_schema.sql src/lib/supabase/database.types.ts
git commit -m "feat(db): migración 046 billing schema + SQL tests + types regen"
git push origin main
```

**Review checkpoint after Task 1.** Verificar deploy Vercel auto, prod sigue verde (LR sin cambios funcionales).

---

## Phase 2 — Pure lib modules (parallelizable después de Task 2)

### Task 2: `src/lib/billing/types.ts` — internal types

**Files:**
- Create: `src/lib/billing/types.ts`

- [ ] **Step 1: Crear file** con types desde DB types + interfaces propias:

```typescript
import type { Database } from "@/lib/supabase/database.types";

export type Plan = Database["public"]["Tables"]["plans"]["Row"];
export type Subscription = Database["public"]["Tables"]["subscriptions"]["Row"];
export type Invoice = Database["public"]["Tables"]["invoices"]["Row"];
export type Payment = Database["public"]["Tables"]["payments"]["Row"];
export type PaymentMethod = Database["public"]["Tables"]["payment_methods"]["Row"];
export type BillingProviderRow = Database["public"]["Tables"]["billing_providers"]["Row"];

export type SubscriptionStatus = "trialing" | "active" | "past_due" | "paused" | "canceled";
export type InvoiceStatus = "draft" | "open" | "paid" | "failed" | "void";
export type PaymentStatus = "pending" | "approved" | "declined" | "error" | "refunded";
export type DianProviderName = "alegra" | "siigo" | "facturatech" | "manual";

export const IVA_RATE = 0.19;                  // 19% Colombia
export const GRACE_DAYS = 7;
export const TRIAL_REMINDER_DAYS_BEFORE = [3, 1];      // T-3, T-1
export const PAST_DUE_REMINDER_DAYS = [1, 3, 5];        // T+1, T+3, T+5
```

- [ ] **Step 2: Commit**

```bash
git add src/lib/billing/types.ts
git commit -m "feat(billing): types module foundation"
```

### Task 3: `src/lib/billing/engine.ts` — period calculations (PARALLEL with T4-T7)

**Files:**
- Create: `src/lib/billing/engine.ts`
- Create: `src/lib/billing/engine.test.ts`

- [ ] **Step 1: Tests primero (TDD)**

```typescript
// src/lib/billing/engine.test.ts
import { describe, it, expect } from "vitest";
import { calculateNextPeriodEnd, calculateIva, isOverEmployeeLimit, shouldPauseAfterGrace } from "./engine";

describe("calculateNextPeriodEnd", () => {
  it("suma 1 mes calendario", () => {
    const r = calculateNextPeriodEnd(new Date("2026-05-24T15:00:00Z"));
    expect(r.toISOString()).toBe("2026-06-24T15:00:00.000Z");
  });
  it("maneja meses con 30/31 días", () => {
    const r = calculateNextPeriodEnd(new Date("2026-01-31T15:00:00Z"));
    // Feb no tiene 31 → JS Date adjusta a mar 03 (overflow)
    expect(r.getMonth()).toBe(2); // marzo (0-indexed)
  });
});

describe("calculateIva", () => {
  it("19% Col", () => {
    expect(calculateIva(100000)).toBe(19000);
  });
  it("redondea al peso entero (sin decimales COP)", () => {
    expect(calculateIva(99999)).toBe(19000); // 18999.81 → round 19000
  });
});

describe("isOverEmployeeLimit", () => {
  it("plan starter max=30 con 30 emp → false", () => {
    expect(isOverEmployeeLimit({ max_employees: 30 } as any, 30)).toBe(false);
  });
  it("plan starter max=30 con 31 emp → true", () => {
    expect(isOverEmployeeLimit({ max_employees: 30 } as any, 31)).toBe(true);
  });
  it("plan enterprise max=null (ilimitado)", () => {
    expect(isOverEmployeeLimit({ max_employees: null } as any, 999)).toBe(false);
  });
});

describe("shouldPauseAfterGrace", () => {
  it("past_due con T+8 → pause", () => {
    const periodEnd = new Date("2026-05-17T00:00:00Z");
    const now = new Date("2026-05-25T00:00:00Z"); // T+8
    expect(shouldPauseAfterGrace(periodEnd, now)).toBe(true);
  });
  it("past_due con T+6 → no pause aún", () => {
    const periodEnd = new Date("2026-05-19T00:00:00Z");
    const now = new Date("2026-05-25T00:00:00Z"); // T+6
    expect(shouldPauseAfterGrace(periodEnd, now)).toBe(false);
  });
});
```

- [ ] **Step 2: Verificar tests fallan** `npm run test -- engine`

- [ ] **Step 3: Implementación**

```typescript
// src/lib/billing/engine.ts
import { IVA_RATE, GRACE_DAYS, type Plan } from "./types";

/**
 * Suma 1 mes calendario. Si el día destino no existe (ej. 31 ene → feb), JS
 * Date overflows al siguiente mes válido — comportamiento aceptado.
 */
export function calculateNextPeriodEnd(currentEnd: Date): Date {
  const next = new Date(currentEnd);
  next.setUTCMonth(next.getUTCMonth() + 1);
  return next;
}

/** 19% IVA Col. Round al peso entero (COP no tiene decimales). */
export function calculateIva(amountCop: number): number {
  return Math.round(amountCop * IVA_RATE);
}

/** Total con IVA. */
export function calculateTotalWithIva(amountCop: number): number {
  return amountCop + calculateIva(amountCop);
}

export function isOverEmployeeLimit(plan: Plan, currentEmployees: number): boolean {
  if (plan.max_employees === null) return false;
  return currentEmployees > plan.max_employees;
}

/** Tras GRACE_DAYS días desde period_end sin pagar → pause. */
export function shouldPauseAfterGrace(periodEnd: Date, now: Date): boolean {
  const msInGrace = GRACE_DAYS * 24 * 60 * 60 * 1000;
  return now.getTime() - periodEnd.getTime() > msInGrace;
}

/** Convierte COP a cents (Wompi requiere integer cents). */
export function copToCents(cop: number): number {
  return cop * 100;
}
```

- [ ] **Step 4: Verificar tests pasan** `npm run test -- engine` → expected ALL PASS

- [ ] **Step 5: Commit**

```bash
git add src/lib/billing/engine.ts src/lib/billing/engine.test.ts
git commit -m "feat(billing): engine — period calcs, IVA, employee limits"
```

### Task 4: `src/lib/billing/dunning.ts` (PARALLEL with T3, T5-T7)

**Files:**
- Create: `src/lib/billing/dunning.ts`
- Create: `src/lib/billing/dunning.test.ts`

- [ ] **Step 1: Tests**

```typescript
import { describe, it, expect } from "vitest";
import { decideDunningAction } from "./dunning";

describe("decideDunningAction", () => {
  it("trialing T-3 → email trial-ending", () => {
    const sub = { status: "trialing", current_period_end: "2026-05-27T00:00:00Z" } as any;
    const now = new Date("2026-05-24T00:00:00Z");
    expect(decideDunningAction(sub, now)).toEqual({ kind: "email", template: "trial-ending", daysOffset: -3 });
  });
  it("trialing T-1 → email trial-ending T-1", () => {
    const sub = { status: "trialing", current_period_end: "2026-05-25T00:00:00Z" } as any;
    const now = new Date("2026-05-24T00:00:00Z");
    expect(decideDunningAction(sub, now)).toEqual({ kind: "email", template: "trial-ending", daysOffset: -1 });
  });
  it("trialing T0 → transition past_due", () => {
    const sub = { status: "trialing", current_period_end: "2026-05-24T00:00:00Z" } as any;
    const now = new Date("2026-05-24T00:00:01Z");
    expect(decideDunningAction(sub, now)).toEqual({ kind: "transition", to: "past_due" });
  });
  it("past_due T+1 → email payment-failed", () => {
    const sub = { status: "past_due", current_period_end: "2026-05-23T00:00:00Z" } as any;
    const now = new Date("2026-05-24T00:00:00Z");
    expect(decideDunningAction(sub, now)).toEqual({ kind: "email", template: "payment-failed", daysOffset: 1 });
  });
  it("past_due T+8 → transition paused", () => {
    const sub = { status: "past_due", current_period_end: "2026-05-16T00:00:00Z" } as any;
    const now = new Date("2026-05-24T00:00:00Z");
    expect(decideDunningAction(sub, now)).toEqual({ kind: "transition", to: "paused" });
  });
  it("active → null (no action)", () => {
    const sub = { status: "active", current_period_end: "2026-06-24T00:00:00Z" } as any;
    expect(decideDunningAction(sub, new Date("2026-05-24"))).toBeNull();
  });
  it("trialing T-2 → null (no reminder ese día)", () => {
    const sub = { status: "trialing", current_period_end: "2026-05-26T00:00:00Z" } as any;
    expect(decideDunningAction(sub, new Date("2026-05-24"))).toBeNull();
  });
});
```

- [ ] **Step 2: Run tests fail**

- [ ] **Step 3: Implementación**

```typescript
// src/lib/billing/dunning.ts
import {
  GRACE_DAYS,
  PAST_DUE_REMINDER_DAYS,
  TRIAL_REMINDER_DAYS_BEFORE,
  type Subscription,
} from "./types";

export type DunningAction =
  | { kind: "email"; template: "trial-ending" | "payment-failed" | "pause-warning"; daysOffset: number }
  | { kind: "transition"; to: "past_due" | "paused" }
  | null;

/** Diferencia en días entre dos Date (UTC), redondeado floor. */
function daysBetween(a: Date, b: Date): number {
  const ms = a.getTime() - b.getTime();
  return Math.floor(ms / (24 * 60 * 60 * 1000));
}

export function decideDunningAction(sub: Subscription, now: Date): DunningAction {
  const periodEnd = new Date(sub.current_period_end);
  const daysFromEnd = daysBetween(now, periodEnd); // negativo si aún no llega

  if (sub.status === "trialing") {
    if (daysFromEnd >= 0) return { kind: "transition", to: "past_due" };
    const daysBefore = -daysFromEnd;
    if (TRIAL_REMINDER_DAYS_BEFORE.includes(daysBefore)) {
      return { kind: "email", template: "trial-ending", daysOffset: -daysBefore };
    }
    return null;
  }

  if (sub.status === "past_due") {
    if (daysFromEnd > GRACE_DAYS) return { kind: "transition", to: "paused" };
    if (PAST_DUE_REMINDER_DAYS.includes(daysFromEnd)) {
      const template = daysFromEnd === 5 ? "pause-warning" : "payment-failed";
      return { kind: "email", template, daysOffset: daysFromEnd };
    }
    return null;
  }

  return null;
}
```

- [ ] **Step 4: Tests PASS**

- [ ] **Step 5: Commit**

```bash
git add src/lib/billing/dunning.ts src/lib/billing/dunning.test.ts
git commit -m "feat(billing): dunning state machine T-3..T+8"
```

### Task 5: `src/lib/billing/crypto.ts` — AES-256-GCM (PARALLEL with T3, T4, T6, T7)

**Files:**
- Create: `src/lib/billing/crypto.ts`
- Create: `src/lib/billing/crypto.test.ts`

- [ ] **Step 1: Tests**

```typescript
import { describe, it, expect, beforeAll } from "vitest";
import { encryptCreds, decryptCreds } from "./crypto";
import { randomBytes } from "crypto";

beforeAll(() => {
  process.env.BILLING_CREDS_ENC_KEY = randomBytes(32).toString("base64");
});

describe("encryptCreds / decryptCreds", () => {
  it("round-trip preserva data", () => {
    const original = { api_key: "secret-key-xyz", email_user: "test@x.com" };
    const enc = encryptCreds(original);
    const dec = decryptCreds(enc);
    expect(dec).toEqual(original);
  });
  it("ciphertext difiere entre encriptaciones (IV aleatorio)", () => {
    const data = { foo: "bar" };
    expect(encryptCreds(data)).not.toBe(encryptCreds(data));
  });
  it("rechaza ciphertext alterado (auth tag)", () => {
    const enc = encryptCreds({ a: "b" });
    const tampered = enc.slice(0, -4) + "XXXX";
    expect(() => decryptCreds(tampered)).toThrow();
  });
});
```

- [ ] **Step 2: Implementación (código exacto del spec sección 6.4)**

Pegar literal el código de `src/lib/billing/crypto.ts` del spec 6.4.

- [ ] **Step 3: Tests PASS**

- [ ] **Step 4: Commit**

```bash
git add src/lib/billing/crypto.ts src/lib/billing/crypto.test.ts
git commit -m "feat(billing): AES-256-GCM crypto para credenciales DIAN"
```

### Task 6: `src/lib/billing/wompi/integrity-hash.ts` (PARALLEL with T3-T5, T7)

**Files:**
- Create: `src/lib/billing/wompi/integrity-hash.ts`
- Create: `src/lib/billing/wompi/integrity-hash.test.ts`

- [ ] **Step 1: Tests con vector conocido del doc Wompi**

```typescript
import { describe, it, expect } from "vitest";
import { computeIntegrityHash } from "./integrity-hash";

describe("computeIntegrityHash", () => {
  it("vector conocido del doc Wompi", () => {
    // Ejemplo doc: ref=sk8-438k4-xmxm392-sn2m24, amount=90000, currency=COP, secret=prod_integrity_Z5mMke9x0k8gpErbDqwrJXMqsI6SFli6
    const hash = computeIntegrityHash({
      reference: "sk8-438k4-xmxm392-sn2m24",
      amountInCents: 9000000,
      currency: "COP",
      integritySecret: "prod_integrity_Z5mMke9x0k8gpErbDqwrJXMqsI6SFli6",
    });
    // Hash exacto que produce este input según docs.wompi.co
    expect(hash).toBe("3b1adb9b22e8d2c5a7a5fae9e2e22a4c1cd6bdc1d28f7d5fb24876fab37f5e4d");
    // NOTA: el hash exacto debe verificarse contra el output real de openssl
    // sha256("sk8-438k4-xmxm3...COPprod_integrity_..."). Si difiere, ajustar test al output real.
  });
  it("inputs distintos producen hashes distintos", () => {
    const h1 = computeIntegrityHash({ reference: "a", amountInCents: 100, currency: "COP", integritySecret: "s" });
    const h2 = computeIntegrityHash({ reference: "a", amountInCents: 200, currency: "COP", integritySecret: "s" });
    expect(h1).not.toBe(h2);
  });
});
```

- [ ] **Step 2: Implementación**

```typescript
// src/lib/billing/wompi/integrity-hash.ts
import { createHash } from "crypto";

export function computeIntegrityHash(input: {
  reference: string;
  amountInCents: number;
  currency: "COP";
  integritySecret: string;
  expirationDate?: string;     // opcional, formato ISO 8601
}): string {
  const parts = [
    input.reference,
    input.amountInCents.toString(),
    input.currency,
    input.expirationDate,        // undefined se omite del concat
    input.integritySecret,
  ].filter(Boolean);
  return createHash("sha256").update(parts.join("")).digest("hex");
}
```

- [ ] **Step 3: Validar hash real contra openssl localmente:**

```bash
echo -n "sk8-438k4-xmxm392-sn2m249000000COPprod_integrity_Z5mMke9x0k8gpErbDqwrJXMqsI6SFli6" | openssl dgst -sha256
```

Ajustar el expected hash en el test al output real.

- [ ] **Step 4: Tests PASS**

- [ ] **Step 5: Commit**

```bash
git add src/lib/billing/wompi/integrity-hash.ts src/lib/billing/wompi/integrity-hash.test.ts
git commit -m "feat(wompi): integrity hash SHA256 para widget"
```

### Task 7: `src/lib/billing/wompi/webhook-verify.ts` (PARALLEL with T3-T6)

**Files:**
- Create: `src/lib/billing/wompi/webhook-verify.ts`
- Create: `src/lib/billing/wompi/webhook-verify.test.ts`

- [ ] **Step 1: Tests**

```typescript
import { describe, it, expect } from "vitest";
import { verifyWompiWebhook } from "./webhook-verify";

const SECRET = "test_events_xxxxx";

describe("verifyWompiWebhook", () => {
  it("payload válido pasa", () => {
    const payload = {
      event: "transaction.updated",
      data: { transaction: { id: "tx-1", status: "APPROVED", amount_in_cents: 100000, currency: "COP" } },
      timestamp: 1700000000,
      signature: { properties: ["transaction.id", "transaction.status", "transaction.amount_in_cents"], checksum: "..." },
    };
    // checksum esperado calculado con sha256("tx-1" + "APPROVED" + "100000" + "1700000000" + SECRET)
    // Calcular vía openssl + pegar acá
    payload.signature.checksum = "<sha256 calculado>";
    expect(verifyWompiWebhook(payload, SECRET)).toBe(true);
  });
  it("checksum incorrecto falla", () => {
    const payload = { /* ... */ signature: { properties: [...], checksum: "deadbeef" } };
    expect(verifyWompiWebhook(payload as any, SECRET)).toBe(false);
  });
  it("missing signature falla", () => {
    expect(verifyWompiWebhook({} as any, SECRET)).toBe(false);
  });
  it("timestamp >30 días falla (anti-replay)", () => {
    const payload = { timestamp: 1, signature: { properties: [], checksum: "..." } };
    expect(verifyWompiWebhook(payload as any, SECRET)).toBe(false);
  });
});
```

- [ ] **Step 2: Implementación**

```typescript
// src/lib/billing/wompi/webhook-verify.ts
import { createHmac, timingSafeEqual } from "crypto";

export type WompiWebhookPayload = {
  event: string;
  data: { transaction: Record<string, unknown> };
  timestamp: number;
  signature: { properties: string[]; checksum: string };
  environment?: string;
  sent_at?: string;
};

const REPLAY_WINDOW_DAYS = 30;

export function verifyWompiWebhook(payload: WompiWebhookPayload, eventsSecret: string): boolean {
  if (!payload?.signature?.checksum || !payload?.timestamp) return false;

  // Anti-replay
  const ageMs = Date.now() - payload.timestamp * 1000;
  if (ageMs > REPLAY_WINDOW_DAYS * 24 * 60 * 60 * 1000) return false;

  // Concat valores en orden definido por signature.properties
  const values: string[] = [];
  for (const prop of payload.signature.properties) {
    const val = getNested(payload.data, prop);
    if (val === undefined) return false;
    values.push(String(val));
  }
  values.push(String(payload.timestamp));
  values.push(eventsSecret);

  const expected = createHmac("sha256", "").update(values.join("")).digest("hex");
  // NOTA Wompi: actually uses sha256 directo, NO hmac. Verificar contra doc.
  const computed = require("crypto").createHash("sha256").update(values.join("")).digest("hex");

  try {
    return timingSafeEqual(Buffer.from(computed), Buffer.from(payload.signature.checksum));
  } catch {
    return false;
  }
}

function getNested(obj: any, path: string): unknown {
  return path.split(".").reduce((acc, key) => acc?.[key], obj);
}
```

**IMPORTANTE:** Wompi usa `sha256(values.join("") + secret)`, NO HMAC. Confirmar contra doc oficial actual antes de finalizar.

- [ ] **Step 3: Tests PASS con vectores ajustados**

- [ ] **Step 4: Commit**

```bash
git add src/lib/billing/wompi/webhook-verify.ts src/lib/billing/wompi/webhook-verify.test.ts
git commit -m "feat(wompi): webhook signature verify + anti-replay"
```

---

## Phase 3 — Wompi REST client + DIAN adapters

### Task 8: `src/lib/billing/wompi/client.ts` — REST client (PARALLEL with T9-T11)

**Files:**
- Create: `src/lib/billing/wompi/client.ts`

- [ ] **Step 1: Implementación (sin tests unitarios — la integración real es Task 25)**

```typescript
// src/lib/billing/wompi/client.ts
const WOMPI_BASE =
  process.env.NODE_ENV === "production" || process.env.VERCEL_ENV === "production"
    ? "https://production.wompi.co/v1"
    : "https://sandbox.wompi.co/v1";

export type AcceptanceTokens = {
  acceptance_token: string;
  accept_personal_auth: string;
  terms_url: string;
  privacy_url: string;
};

export async function getAcceptanceTokens(): Promise<AcceptanceTokens> {
  const publicKey = process.env.NEXT_PUBLIC_WOMPI_PUBLIC_KEY;
  if (!publicKey) throw new Error("NEXT_PUBLIC_WOMPI_PUBLIC_KEY not set");
  const res = await fetch(`${WOMPI_BASE}/merchants/${publicKey}`);
  if (!res.ok) throw new Error(`Wompi /merchants ${res.status}`);
  const json = await res.json();
  return {
    acceptance_token: json.data.presigned_acceptance.acceptance_token,
    accept_personal_auth: json.data.presigned_personal_data_auth.acceptance_token,
    terms_url: json.data.presigned_acceptance.permalink,
    privacy_url: json.data.presigned_personal_data_auth.permalink,
  };
}

export type ChargeInput = {
  paymentSourceId: string;
  amountInCents: number;
  currency: "COP";
  reference: string;     // invoice.id
  customerEmail: string;
  recurrent: boolean;
};

export async function createTransaction(input: ChargeInput): Promise<{
  id: string;
  status: "APPROVED" | "DECLINED" | "PENDING" | "ERROR" | "VOIDED";
  reference: string;
}> {
  const privateKey = process.env.WOMPI_PRIVATE_KEY;
  if (!privateKey) throw new Error("WOMPI_PRIVATE_KEY not set");

  const res = await fetch(`${WOMPI_BASE}/transactions`, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "Authorization": `Bearer ${privateKey}`,
    },
    body: JSON.stringify({
      amount_in_cents: input.amountInCents,
      currency: input.currency,
      reference: input.reference,
      payment_source_id: input.paymentSourceId,
      customer_email: input.customerEmail,
      payment_method: { type: "CARD", installments: 1 },
      recurrent: input.recurrent,
    }),
  });

  if (!res.ok) {
    const text = await res.text();
    throw new Error(`Wompi /transactions ${res.status}: ${text}`);
  }

  const json = await res.json();
  return { id: json.data.id, status: json.data.status, reference: json.data.reference };
}

export async function getTransaction(transactionId: string) {
  const privateKey = process.env.WOMPI_PRIVATE_KEY;
  const res = await fetch(`${WOMPI_BASE}/transactions/${transactionId}`, {
    headers: { "Authorization": `Bearer ${privateKey}` },
  });
  if (!res.ok) throw new Error(`Wompi /transactions/${transactionId} ${res.status}`);
  return res.json();
}
```

- [ ] **Step 2: Commit**

```bash
git add src/lib/billing/wompi/client.ts
git commit -m "feat(wompi): REST client (acceptance, transactions)"
```

### Task 9: `src/lib/billing/providers/types.ts` + factory (PARALLEL with T8, T10, T11)

**Files:**
- Create: `src/lib/billing/providers/types.ts`
- Create: `src/lib/billing/providers/index.ts`

- [ ] **Step 1: Interface**

```typescript
// src/lib/billing/providers/types.ts
import type { Invoice } from "../types";
import type { Database } from "@/lib/supabase/database.types";

export type Organization = Database["public"]["Tables"]["organizations"]["Row"];

export type EmitResult = {
  externalId: string;
  pdfUrl: string | null;
  status: "pending" | "accepted";
};

export interface BillingProvider {
  emitInvoice(invoice: Invoice, customer: Organization): Promise<EmitResult>;
  voidInvoice(externalId: string): Promise<boolean>;
  getStatus(externalId: string): Promise<"pending" | "accepted" | "rejected">;
}
```

- [ ] **Step 2: Factory**

```typescript
// src/lib/billing/providers/index.ts
import { createAdminClient } from "@/lib/supabase/admin";
import { decryptCreds } from "../crypto";
import { AlegraProvider } from "./alegra";
import { ManualProvider } from "./manual";
import type { BillingProvider } from "./types";
import type { DianProviderName } from "../types";

export async function getProvider(orgId: string): Promise<BillingProvider> {
  const supabase = createAdminClient();
  const { data } = await supabase
    .from("billing_providers")
    .select("provider, config, is_active")
    .eq("organization_id", orgId)
    .maybeSingle();

  if (!data || !data.is_active) return new ManualProvider();

  switch (data.provider as DianProviderName) {
    case "alegra":
      return new AlegraProvider(decryptCreds(data.config as unknown as string));
    case "manual":
      return new ManualProvider();
    default:
      throw new Error(`Unsupported DIAN provider: ${data.provider}`);
  }
}

export type { BillingProvider, EmitResult, Organization } from "./types";
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/billing/providers/types.ts src/lib/billing/providers/index.ts
git commit -m "feat(dian): provider interface + factory"
```

### Task 10: `src/lib/billing/providers/manual.ts` (PARALLEL with T8, T9, T11)

**Files:**
- Create: `src/lib/billing/providers/manual.ts`
- Create: `src/lib/billing/providers/manual.test.ts`

- [ ] **Step 1: Tests**

```typescript
import { describe, it, expect } from "vitest";
import { ManualProvider } from "./manual";

describe("ManualProvider", () => {
  it("emitInvoice retorna externalId con prefijo manual-", async () => {
    const p = new ManualProvider();
    const res = await p.emitInvoice({ id: "inv-1" } as any, { id: "org-1" } as any);
    expect(res.externalId).toMatch(/^manual-/);
    expect(res.pdfUrl).toBeNull();
    expect(res.status).toBe("pending");
  });
  it("voidInvoice retorna true", async () => {
    expect(await new ManualProvider().voidInvoice("manual-1")).toBe(true);
  });
  it("getStatus retorna pending por default", async () => {
    expect(await new ManualProvider().getStatus("manual-1")).toBe("pending");
  });
});
```

- [ ] **Step 2: Implementación**

```typescript
// src/lib/billing/providers/manual.ts
import { randomUUID } from "crypto";
import type { BillingProvider, EmitResult, Organization } from "./types";
import type { Invoice } from "../types";

export class ManualProvider implements BillingProvider {
  async emitInvoice(_invoice: Invoice, _customer: Organization): Promise<EmitResult> {
    return {
      externalId: `manual-${randomUUID()}`,
      pdfUrl: null,
      status: "pending",
    };
  }
  async voidInvoice(_externalId: string): Promise<boolean> {
    return true;
  }
  async getStatus(_externalId: string): Promise<"pending" | "accepted" | "rejected"> {
    return "pending";
  }
}
```

- [ ] **Step 3: Tests PASS**

- [ ] **Step 4: Commit**

```bash
git add src/lib/billing/providers/manual.ts src/lib/billing/providers/manual.test.ts
git commit -m "feat(dian): manual provider (no-op)"
```

### Task 11: `src/lib/billing/providers/alegra.ts` (PARALLEL with T8-T10)

**Files:**
- Create: `src/lib/billing/providers/alegra.ts`
- Create: `src/lib/billing/providers/alegra.test.ts`

- [ ] **Step 1: Tests con fetch mockeado**

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { AlegraProvider } from "./alegra";

beforeEach(() => {
  vi.restoreAllMocks();
});

describe("AlegraProvider", () => {
  it("emitInvoice ok → externalId + pdfUrl", async () => {
    global.fetch = vi.fn().mockResolvedValue({
      ok: true,
      json: async () => ({ id: "ale-123", pdf: "https://alegra.com/pdf/123.pdf" }),
    } as any);

    const p = new AlegraProvider({ api_key: "k", email_user: "e@x.com" });
    const res = await p.emitInvoice(
      { id: "inv-1", amount_cop: 100000, iva_cop: 19000, total_cop: 119000 } as any,
      { id: "org-1", legal_name: "Acme SAS", nit: "900-1" } as any
    );
    expect(res.externalId).toBe("ale-123");
    expect(res.pdfUrl).toContain("alegra.com");
    expect(res.status).toBe("accepted");
  });

  it("emitInvoice 401 → throw error específico", async () => {
    global.fetch = vi.fn().mockResolvedValue({ ok: false, status: 401, text: async () => "Unauthorized" } as any);
    const p = new AlegraProvider({ api_key: "bad", email_user: "e@x.com" });
    await expect(p.emitInvoice({ id: "inv-1" } as any, { nit: "1" } as any)).rejects.toThrow(/401/);
  });

  it("emitInvoice 500 → throw retryable", async () => {
    global.fetch = vi.fn().mockResolvedValue({ ok: false, status: 500, text: async () => "ISE" } as any);
    await expect(new AlegraProvider({ api_key: "k", email_user: "e@x.com" }).emitInvoice({} as any, {} as any)).rejects.toThrow(/500/);
  });
});
```

- [ ] **Step 2: Implementación**

```typescript
// src/lib/billing/providers/alegra.ts
import type { BillingProvider, EmitResult, Organization } from "./types";
import type { Invoice } from "../types";

const ALEGRA_BASE = "https://api.alegra.com/api/v1";

type AlegraCreds = { api_key: string; email_user: string };

export class AlegraProvider implements BillingProvider {
  constructor(private creds: AlegraCreds) {}

  private authHeader(): string {
    const token = Buffer.from(`${this.creds.email_user}:${this.creds.api_key}`).toString("base64");
    return `Basic ${token}`;
  }

  async emitInvoice(invoice: Invoice, customer: Organization): Promise<EmitResult> {
    const body = {
      date: new Date().toISOString().slice(0, 10),
      dueDate: new Date(invoice.due_date).toISOString().slice(0, 10),
      client: {
        identification: customer.nit ?? "222222222",
        name: customer.legal_name ?? customer.name,
      },
      items: [{
        name: `Suscripción Tus Horarios — ${invoice.plan_id}`,
        price: invoice.amount_cop,
        quantity: 1,
        tax: [{ id: 1 }],   // IVA 19% Col en Alegra
      }],
      observations: `Período ${invoice.period_start} a ${invoice.period_end}`,
    };

    const res = await fetch(`${ALEGRA_BASE}/invoices`, {
      method: "POST",
      headers: {
        "Authorization": this.authHeader(),
        "Content-Type": "application/json",
      },
      body: JSON.stringify(body),
    });

    if (!res.ok) {
      const text = await res.text();
      throw new Error(`Alegra ${res.status}: ${text}`);
    }

    const json = await res.json();
    return {
      externalId: String(json.id),
      pdfUrl: json.pdf ?? null,
      status: "accepted",
    };
  }

  async voidInvoice(externalId: string): Promise<boolean> {
    const res = await fetch(`${ALEGRA_BASE}/invoices/${externalId}/void`, {
      method: "POST",
      headers: { "Authorization": this.authHeader() },
    });
    return res.ok;
  }

  async getStatus(externalId: string): Promise<"pending" | "accepted" | "rejected"> {
    const res = await fetch(`${ALEGRA_BASE}/invoices/${externalId}`, {
      headers: { "Authorization": this.authHeader() },
    });
    if (!res.ok) return "rejected";
    const json = await res.json();
    return json.status === "open" || json.status === "closed" ? "accepted" : "pending";
  }
}
```

- [ ] **Step 3: Tests PASS**

- [ ] **Step 4: Commit**

```bash
git add src/lib/billing/providers/alegra.ts src/lib/billing/providers/alegra.test.ts
git commit -m "feat(dian): Alegra provider adapter + tests"
```

**Review checkpoint after Phase 3.** Tests 386 + 50 nuevos (~436) deben pasar. Build verde.

---

## Phase 4 — API endpoints

### Task 12: API `/api/billing/wompi/acceptance` + `/prepare-checkout`

**Files:**
- Create: `src/app/api/billing/wompi/acceptance/route.ts`
- Create: `src/app/api/billing/wompi/prepare-checkout/route.ts`

- [ ] **Step 1: Acceptance endpoint con cache 50min**

```typescript
// src/app/api/billing/wompi/acceptance/route.ts
import { NextResponse } from "next/server";
import { getAcceptanceTokens } from "@/lib/billing/wompi/client";

let cache: { value: Awaited<ReturnType<typeof getAcceptanceTokens>>; expiresAt: number } | null = null;
const TTL_MS = 50 * 60 * 1000;

export const runtime = "nodejs";

export async function GET() {
  if (cache && cache.expiresAt > Date.now()) {
    return NextResponse.json(cache.value);
  }
  try {
    const tokens = await getAcceptanceTokens();
    cache = { value: tokens, expiresAt: Date.now() + TTL_MS };
    return NextResponse.json(tokens);
  } catch (err) {
    console.error("[acceptance]", err);
    return NextResponse.json({ error: "Wompi unavailable" }, { status: 503 });
  }
}
```

- [ ] **Step 2: Prepare-checkout (computa integrity hash + crea draft invoice)**

```typescript
// src/app/api/billing/wompi/prepare-checkout/route.ts
import { NextResponse } from "next/server";
import { z } from "zod";
import { createClient } from "@/lib/supabase/server";
import { createAdminClient } from "@/lib/supabase/admin";
import { canAdmin } from "@/lib/auth/can-manage";
import { computeIntegrityHash } from "@/lib/billing/wompi/integrity-hash";
import { calculateIva, calculateTotalWithIva, calculateNextPeriodEnd, copToCents } from "@/lib/billing/engine";

const Schema = z.object({ planId: z.string() });
export const runtime = "nodejs";

export async function POST(req: Request) {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "no auth" }, { status: 401 });

  const { data: profile } = await supabase
    .from("profiles")
    .select("role, organization_id, organizations!profiles_org_fk(name, nit, billing_email, billing_exempt)")
    .eq("id", user.id)
    .maybeSingle();

  if (!canAdmin(profile?.role)) return NextResponse.json({ error: "forbidden" }, { status: 403 });
  if (!profile?.organization_id) return NextResponse.json({ error: "no org" }, { status: 400 });

  const orgJoin: any = Array.isArray((profile as any).organizations) ? (profile as any).organizations[0] : (profile as any).organizations;
  if (orgJoin?.billing_exempt) return NextResponse.json({ error: "Org exenta de billing" }, { status: 400 });
  if (!orgJoin?.nit) return NextResponse.json({ error: "Configura NIT en la organización antes de pagar" }, { status: 400 });

  const body = Schema.parse(await req.json());
  const admin = createAdminClient();
  const { data: plan, error: planErr } = await admin.from("plans").select("*").eq("id", body.planId).maybeSingle();
  if (planErr || !plan) return NextResponse.json({ error: "Plan no encontrado" }, { status: 404 });
  if (plan.contact_sales) return NextResponse.json({ error: "Plan requiere contacto comercial" }, { status: 400 });

  const amount = plan.price_cop;
  const iva = calculateIva(amount);
  const total = calculateTotalWithIva(amount);

  // Crea/encuentra subscription draft
  const periodStart = new Date();
  const periodEnd = calculateNextPeriodEnd(periodStart);

  const { data: existingSub } = await admin.from("subscriptions").select("id").eq("organization_id", profile.organization_id).maybeSingle();
  let subscriptionId: string;
  if (existingSub) {
    await admin.from("subscriptions").update({ plan_id: body.planId }).eq("id", existingSub.id);
    subscriptionId = existingSub.id;
  } else {
    const { data: newSub } = await admin.from("subscriptions").insert({
      organization_id: profile.organization_id,
      plan_id: body.planId,
      status: "trialing",
      current_period_start: periodStart.toISOString(),
      current_period_end: periodEnd.toISOString(),
    }).select("id").single();
    subscriptionId = newSub!.id;
  }

  // Crea invoice draft
  const { data: invoice } = await admin.from("invoices").insert({
    organization_id: profile.organization_id,
    subscription_id: subscriptionId,
    plan_id: body.planId,
    period_start: periodStart.toISOString(),
    period_end: periodEnd.toISOString(),
    amount_cop: amount,
    iva_cop: iva,
    total_cop: total,
    status: "open",
    due_date: periodStart.toISOString(),
  }).select("id").single();

  const reference = invoice!.id;
  const amountInCents = copToCents(total);
  const integritySignature = computeIntegrityHash({
    reference,
    amountInCents,
    currency: "COP",
    integritySecret: process.env.WOMPI_INTEGRITY_SECRET!,
  });

  return NextResponse.json({
    publicKey: process.env.NEXT_PUBLIC_WOMPI_PUBLIC_KEY,
    reference,
    amountInCents,
    currency: "COP",
    signature: integritySignature,
    customerEmail: orgJoin.billing_email ?? user.email,
    redirectUrl: `${process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.tushorarios.com"}/facturacion?checkout=complete`,
  });
}
```

- [ ] **Step 3: Build + lint**

```bash
npm run build 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add src/app/api/billing/wompi/
git commit -m "feat(api): billing/wompi acceptance + prepare-checkout"
```

### Task 13: API `/api/billing/payment-methods` (GET list + DELETE)

**Files:**
- Create: `src/app/api/billing/payment-methods/route.ts`

- [ ] **Step 1: Implementación GET + DELETE (POST = creado por webhook handler, no por user)**

```typescript
// src/app/api/billing/payment-methods/route.ts
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { canAdmin } from "@/lib/auth/can-manage";

export const runtime = "nodejs";

export async function GET() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "no auth" }, { status: 401 });

  const { data: profile } = await supabase.from("profiles").select("role, organization_id").eq("id", user.id).maybeSingle();
  if (!canAdmin(profile?.role)) return NextResponse.json({ error: "forbidden" }, { status: 403 });

  const { data, error } = await supabase
    .from("payment_methods")
    .select("id, card_brand, card_last4, card_exp_month, card_exp_year, is_default, created_at")
    .order("created_at", { ascending: false });

  if (error) return NextResponse.json({ error: error.message }, { status: 500 });
  return NextResponse.json({ data });
}

export async function DELETE(req: Request) {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "no auth" }, { status: 401 });

  const { data: profile } = await supabase.from("profiles").select("role").eq("id", user.id).maybeSingle();
  if (!canAdmin(profile?.role)) return NextResponse.json({ error: "forbidden" }, { status: 403 });

  const url = new URL(req.url);
  const id = url.searchParams.get("id");
  if (!id) return NextResponse.json({ error: "id required" }, { status: 400 });

  const { error } = await supabase.from("payment_methods").delete().eq("id", id);
  if (error) return NextResponse.json({ error: error.message }, { status: 500 });
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/api/billing/payment-methods/route.ts
git commit -m "feat(api): billing payment-methods GET/DELETE"
```

### Task 14: API `/api/billing/subscriptions` + `/cancel`

**Files:**
- Create: `src/app/api/billing/subscriptions/route.ts`
- Create: `src/app/api/billing/subscriptions/cancel/route.ts`

- [ ] **Step 1: GET subscription actual**

```typescript
// src/app/api/billing/subscriptions/route.ts
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { canAdmin } from "@/lib/auth/can-manage";

export const runtime = "nodejs";

export async function GET() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "no auth" }, { status: 401 });

  const { data: profile } = await supabase.from("profiles").select("role, organization_id").eq("id", user.id).maybeSingle();
  if (!canAdmin(profile?.role)) return NextResponse.json({ error: "forbidden" }, { status: 403 });
  if (!profile?.organization_id) return NextResponse.json({ data: null });

  const { data } = await supabase
    .from("subscriptions")
    .select("*, plans!subscriptions_plan_id_fkey(id, name, price_cop, max_employees)")
    .eq("organization_id", profile.organization_id)
    .maybeSingle();

  return NextResponse.json({ data });
}
```

- [ ] **Step 2: Cancel endpoint (cancel_at_period_end)**

```typescript
// src/app/api/billing/subscriptions/cancel/route.ts
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { canAdmin } from "@/lib/auth/can-manage";

export const runtime = "nodejs";

export async function POST() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "no auth" }, { status: 401 });

  const { data: profile } = await supabase.from("profiles").select("role, organization_id").eq("id", user.id).maybeSingle();
  if (!canAdmin(profile?.role)) return NextResponse.json({ error: "forbidden" }, { status: 403 });
  if (!profile?.organization_id) return NextResponse.json({ error: "no org" }, { status: 400 });

  const { data, error } = await supabase
    .from("subscriptions")
    .update({ cancel_at_period_end: true })
    .eq("organization_id", profile.organization_id)
    .select("current_period_end")
    .single();

  if (error) return NextResponse.json({ error: error.message }, { status: 500 });
  return NextResponse.json({ ok: true, effective: data.current_period_end });
}
```

- [ ] **Step 3: Commit**

```bash
git add src/app/api/billing/subscriptions/
git commit -m "feat(api): billing subscriptions GET + cancel"
```

### Task 15: API `/api/billing/providers` (DIAN config) + `/invoices` (history)

**Files:**
- Create: `src/app/api/billing/providers/route.ts`
- Create: `src/app/api/billing/invoices/route.ts`

- [ ] **Step 1: providers PUT (set DIAN config con encrypt)**

```typescript
// src/app/api/billing/providers/route.ts
import { NextResponse } from "next/server";
import { z } from "zod";
import { createClient } from "@/lib/supabase/server";
import { createAdminClient } from "@/lib/supabase/admin";
import { canAdmin } from "@/lib/auth/can-manage";
import { encryptCreds } from "@/lib/billing/crypto";

const Schema = z.object({
  provider: z.enum(["alegra", "siigo", "facturatech", "manual"]),
  config: z.record(z.string()),
});

export const runtime = "nodejs";

export async function GET() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "no auth" }, { status: 401 });

  const { data: profile } = await supabase.from("profiles").select("role, organization_id").eq("id", user.id).maybeSingle();
  if (!canAdmin(profile?.role)) return NextResponse.json({ error: "forbidden" }, { status: 403 });
  if (!profile?.organization_id) return NextResponse.json({ data: null });

  const { data } = await supabase
    .from("billing_providers")
    .select("provider, is_active, configured_at")
    .eq("organization_id", profile.organization_id)
    .maybeSingle();

  return NextResponse.json({ data });   // NUNCA expone config (encrypted creds)
}

export async function PUT(req: Request) {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "no auth" }, { status: 401 });

  const { data: profile } = await supabase.from("profiles").select("role, organization_id").eq("id", user.id).maybeSingle();
  if (!canAdmin(profile?.role)) return NextResponse.json({ error: "forbidden" }, { status: 403 });
  if (!profile?.organization_id) return NextResponse.json({ error: "no org" }, { status: 400 });

  const body = Schema.parse(await req.json());
  const encrypted = encryptCreds(body.config);

  const admin = createAdminClient();
  const { error } = await admin.from("billing_providers").upsert({
    organization_id: profile.organization_id,
    provider: body.provider,
    config: encrypted,
    is_active: true,
    updated_at: new Date().toISOString(),
  }, { onConflict: "organization_id" });

  if (error) return NextResponse.json({ error: error.message }, { status: 500 });
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 2: invoices GET (history table)**

```typescript
// src/app/api/billing/invoices/route.ts
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { canAdmin } from "@/lib/auth/can-manage";

export const runtime = "nodejs";

export async function GET() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "no auth" }, { status: 401 });

  const { data: profile } = await supabase.from("profiles").select("role, organization_id").eq("id", user.id).maybeSingle();
  if (!canAdmin(profile?.role)) return NextResponse.json({ error: "forbidden" }, { status: 403 });
  if (!profile?.organization_id) return NextResponse.json({ data: [] });

  const { data } = await supabase
    .from("invoices")
    .select("id, plan_id, period_start, period_end, amount_cop, iva_cop, total_cop, status, paid_at, dian_pdf_url, dian_status, created_at")
    .eq("organization_id", profile.organization_id)
    .order("created_at", { ascending: false })
    .limit(100);

  return NextResponse.json({ data });
}
```

- [ ] **Step 3: Commit**

```bash
git add src/app/api/billing/providers/ src/app/api/billing/invoices/
git commit -m "feat(api): billing DIAN providers config + invoices history"
```

---

## Phase 5 — Webhook handler (CORE)

### Task 16: `/api/webhooks/wompi` — orchestrator

**Files:**
- Create: `src/app/api/webhooks/wompi/route.ts`

- [ ] **Step 1: Implementación completa**

```typescript
// src/app/api/webhooks/wompi/route.ts
import { NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/admin";
import { verifyWompiWebhook, type WompiWebhookPayload } from "@/lib/billing/wompi/webhook-verify";
import { getTransaction } from "@/lib/billing/wompi/client";
import { calculateNextPeriodEnd } from "@/lib/billing/engine";

export const runtime = "nodejs";

export async function POST(req: Request) {
  const raw = await req.text();
  let payload: WompiWebhookPayload;
  try {
    payload = JSON.parse(raw);
  } catch {
    return NextResponse.json({ error: "invalid json" }, { status: 400 });
  }

  const secret = process.env.WOMPI_EVENTS_SECRET;
  if (!secret) return NextResponse.json({ error: "secret not configured" }, { status: 500 });
  if (!verifyWompiWebhook(payload, secret)) return NextResponse.json({ error: "invalid signature" }, { status: 401 });

  if (payload.event !== "transaction.updated") return NextResponse.json({ ok: true, ignored: true });

  const tx = payload.data.transaction as any;
  const supabase = createAdminClient();

  // Idempotencia: payment con provider_transaction_id UNIQUE
  const { data: existing } = await supabase
    .from("payments")
    .select("id, invoice_id, status")
    .eq("provider_transaction_id", tx.id)
    .maybeSingle();

  const invoice = existing
    ? (await supabase.from("invoices").select("*").eq("id", existing.invoice_id).single()).data
    : (await supabase.from("invoices").select("*").eq("id", tx.reference).single()).data;

  if (!invoice) {
    console.error("[webhook wompi] invoice not found for reference", tx.reference);
    return NextResponse.json({ ok: true, warn: "invoice not found" });
  }

  // Insert/update payment row
  if (existing) {
    await supabase.from("payments").update({
      status: mapStatus(tx.status),
      failure_reason: tx.status_message ?? null,
      completed_at: new Date().toISOString(),
    }).eq("id", existing.id);
  } else {
    await supabase.from("payments").insert({
      invoice_id: invoice.id,
      payment_method_id: invoice.payment_method_id ?? null,
      provider: "wompi",
      provider_transaction_id: tx.id,
      amount_cop: Math.round(tx.amount_in_cents / 100),
      status: mapStatus(tx.status),
      failure_reason: tx.status_message ?? null,
      completed_at: new Date().toISOString(),
    });
  }

  // Side effects por status
  if (tx.status === "APPROVED") {
    // Marca invoice paid
    await supabase.from("invoices").update({ status: "paid", paid_at: new Date().toISOString() }).eq("id", invoice.id);

    // Si es 1er charge: extrae payment_source_id y guarda payment_method
    const fullTx = await getTransaction(tx.id).catch(() => null);
    const psId = fullTx?.data?.payment_source_id;
    if (psId) {
      const { data: pm } = await supabase.from("payment_methods").upsert({
        organization_id: invoice.organization_id,
        provider: "wompi",
        provider_payment_source_id: String(psId),
        card_brand: fullTx?.data?.payment_method?.extra?.brand ?? null,
        card_last4: fullTx?.data?.payment_method?.extra?.last_four ?? null,
        card_exp_month: parseInt(fullTx?.data?.payment_method?.extra?.exp_month ?? "0") || null,
        card_exp_year: 2000 + parseInt(fullTx?.data?.payment_method?.extra?.exp_year ?? "0") || null,
        is_default: true,
      }, { onConflict: "organization_id,provider_payment_source_id" }).select("id").single();

      // Link a subscription si no tiene payment_method
      if (pm) {
        await supabase.from("subscriptions").update({
          payment_method_id: pm.id,
          status: "active",
          current_period_end: calculateNextPeriodEnd(new Date(invoice.period_start)).toISOString(),
        }).eq("organization_id", invoice.organization_id);
      }
    } else {
      // Recurring charge — solo extiende period
      await supabase.from("subscriptions").update({
        status: "active",
        current_period_end: calculateNextPeriodEnd(new Date(invoice.period_start)).toISOString(),
      }).eq("organization_id", invoice.organization_id);
    }

    // Encola DIAN emit (Task 17 — async)
    fireDianEmitJob(invoice.id).catch((e) => console.error("[dian-emit-job]", e));

    // Email confirm (Task 18 — Resend)
    fireBillingEmail("payment-confirmed", invoice.organization_id, { invoiceId: invoice.id }).catch((e) =>
      console.error("[email]", e)
    );
  } else if (tx.status === "DECLINED" || tx.status === "ERROR" || tx.status === "VOIDED") {
    await supabase.from("invoices").update({ status: "failed", retry_count: (invoice.retry_count ?? 0) + 1 }).eq("id", invoice.id);
    await supabase.from("subscriptions").update({ status: "past_due" }).eq("organization_id", invoice.organization_id);
  }

  return NextResponse.json({ ok: true });
}

function mapStatus(wompiStatus: string): "pending" | "approved" | "declined" | "error" | "refunded" {
  switch (wompiStatus) {
    case "APPROVED": return "approved";
    case "DECLINED": return "declined";
    case "VOIDED": return "refunded";
    case "ERROR": return "error";
    default: return "pending";
  }
}

// Stubs — Tasks 17 + 18 los implementan
async function fireDianEmitJob(_invoiceId: string): Promise<void> { /* Task 17 */ }
async function fireBillingEmail(_template: string, _orgId: string, _data: unknown): Promise<void> { /* Task 18 */ }
```

- [ ] **Step 2: Build + lint**

```bash
npm run build 2>&1 | tail -5
```

- [ ] **Step 3: Commit**

```bash
git add src/app/api/webhooks/wompi/route.ts
git commit -m "feat(webhook): Wompi handler orchestrator (idempotent, side-effects)"
```

**Review checkpoint after Task 16.** Webhook es el corazón. Review especialmente: idempotencia, side effects, manejo de payment_source_id en 1er vs N-th charge.

---

## Phase 6 — Cron jobs + DIAN async + emails

### Task 17: DIAN async emit job + `process_dian_emit_job` table

**Files:**
- Create: `supabase/migrations/047_dian_emit_jobs.sql`
- Create: `supabase/tests/dian_jobs_schema.sql`
- Create: `src/lib/billing/dian-emit-job.ts`
- Create: `src/lib/billing/dian-emit-job.test.ts`
- Modify: `src/app/api/webhooks/wompi/route.ts` (reemplazar stub `fireDianEmitJob`)

- [ ] **Step 1: Migración table-based queue**

```sql
-- supabase/migrations/047_dian_emit_jobs.sql
BEGIN;
CREATE TABLE dian_emit_jobs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  invoice_id UUID NOT NULL REFERENCES invoices(id) ON DELETE CASCADE,
  status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','processing','succeeded','failed')),
  attempt_count INT NOT NULL DEFAULT 0,
  next_attempt_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  last_error TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX dian_jobs_next_attempt_idx ON dian_emit_jobs(status, next_attempt_at);
ALTER TABLE dian_emit_jobs ENABLE ROW LEVEL SECURITY;
CREATE POLICY dian_jobs_super_admin ON dian_emit_jobs FOR ALL TO authenticated
  USING (is_super_admin()) WITH CHECK (is_super_admin());
COMMIT;
```

- [ ] **Step 2: Tests sql + apply** (mismo patrón Task 1)

- [ ] **Step 3: `dian-emit-job.ts` — encoder + processor**

```typescript
// src/lib/billing/dian-emit-job.ts
import { createAdminClient } from "@/lib/supabase/admin";
import { getProvider } from "./providers";

const RETRY_DELAYS_MS = [60_000, 5 * 60_000, 30 * 60_000];
const MAX_ATTEMPTS = 3;

export async function enqueueDianEmitJob(invoiceId: string): Promise<void> {
  const supabase = createAdminClient();
  await supabase.from("dian_emit_jobs").insert({ invoice_id: invoiceId });
}

export async function processDianEmitJobs(): Promise<{ processed: number; succeeded: number; failed: number }> {
  const supabase = createAdminClient();
  const { data: jobs } = await supabase
    .from("dian_emit_jobs")
    .select("*")
    .in("status", ["pending"])
    .lte("next_attempt_at", new Date().toISOString())
    .limit(10);

  let succeeded = 0, failed = 0;

  for (const job of jobs ?? []) {
    await supabase.from("dian_emit_jobs").update({ status: "processing", updated_at: new Date().toISOString() }).eq("id", job.id);

    try {
      const { data: invoice } = await supabase.from("invoices").select("*").eq("id", job.invoice_id).single();
      const { data: org } = await supabase.from("organizations").select("*").eq("id", invoice!.organization_id).single();
      const provider = await getProvider(invoice!.organization_id);
      const result = await provider.emitInvoice(invoice as any, org as any);

      await supabase.from("invoices").update({
        dian_provider: (await supabase.from("billing_providers").select("provider").eq("organization_id", invoice!.organization_id).maybeSingle()).data?.provider ?? "manual",
        dian_invoice_id: result.externalId,
        dian_pdf_url: result.pdfUrl,
        dian_status: result.status === "accepted" ? "accepted" : "pending",
      }).eq("id", job.invoice_id);

      await supabase.from("dian_emit_jobs").update({ status: "succeeded", updated_at: new Date().toISOString() }).eq("id", job.id);
      succeeded++;
    } catch (err: any) {
      const newAttempt = job.attempt_count + 1;
      const isMaxed = newAttempt >= MAX_ATTEMPTS;
      await supabase.from("dian_emit_jobs").update({
        status: isMaxed ? "failed" : "pending",
        attempt_count: newAttempt,
        next_attempt_at: isMaxed
          ? job.next_attempt_at
          : new Date(Date.now() + RETRY_DELAYS_MS[newAttempt - 1]).toISOString(),
        last_error: err.message ?? String(err),
        updated_at: new Date().toISOString(),
      }).eq("id", job.id);
      failed++;

      if (isMaxed) {
        // TODO Task 18: notificar super_admin vía email
        console.error("[dian-emit-job] max attempts reached for invoice", job.invoice_id);
      }
    }
  }

  return { processed: jobs?.length ?? 0, succeeded, failed };
}
```

- [ ] **Step 4: Tests con mocks de providers + supabase**

(Patrón: mockear `getProvider`, mockear supabase admin client. Casos: success, retry, max-attempts.)

- [ ] **Step 5: Reemplazar stub en webhook handler**

En `src/app/api/webhooks/wompi/route.ts` línea con `fireDianEmitJob`, reemplazar por:

```typescript
import { enqueueDianEmitJob } from "@/lib/billing/dian-emit-job";
// ...
enqueueDianEmitJob(invoice.id).catch((e) => console.error("[dian-emit-job]", e));
```

(Y borrar el stub local.)

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/047_dian_emit_jobs.sql supabase/tests/dian_jobs_schema.sql src/lib/billing/dian-emit-job.ts src/lib/billing/dian-emit-job.test.ts src/app/api/webhooks/wompi/route.ts src/lib/supabase/database.types.ts
git commit -m "feat(dian): async emit job table + processor + retries exponenciales"
```

### Task 18: Email templates + helper + tabla `sent_reminders`

**Files:**
- Create: `supabase/migrations/048_sent_reminders.sql`
- Create: `src/emails/billing-trial-ending.tsx`
- Create: `src/emails/billing-payment-failed.tsx`
- Create: `src/emails/billing-pause-warning.tsx`
- Create: `src/emails/billing-payment-confirmed.tsx`
- Create: `src/lib/emails/send-billing-emails.ts`
- Modify: `src/app/api/webhooks/wompi/route.ts` (reemplazar stub `fireBillingEmail`)

- [ ] **Step 1: Migración sent_reminders (idempotencia)**

```sql
-- supabase/migrations/048_sent_reminders.sql
BEGIN;
CREATE TABLE sent_reminders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
  template TEXT NOT NULL,
  days_offset INT NOT NULL,
  sent_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE (organization_id, template, days_offset)
);
ALTER TABLE sent_reminders ENABLE ROW LEVEL SECURITY;
CREATE POLICY sr_super_admin ON sent_reminders FOR ALL TO authenticated
  USING (is_super_admin()) WITH CHECK (is_super_admin());
COMMIT;
```

- [ ] **Step 2: Aplicar migración + regen types**

- [ ] **Step 3: 4 email templates** (basados en patrón existente `src/emails/demo-request-confirmation.tsx`):

`billing-trial-ending.tsx`, `billing-payment-failed.tsx`, `billing-pause-warning.tsx`, `billing-payment-confirmed.tsx`.

Cada uno acepta props (org_name, plan_name, amount_cop, days_until_X, payment_url, invoice_pdf_url según corresponda). El subagent escribe los 4 siguiendo el styling existente.

- [ ] **Step 4: Helper `send-billing-emails.ts`**

```typescript
// src/lib/emails/send-billing-emails.ts
import { resend, FROM_NOTIF } from "@/lib/resend";
import BillingTrialEndingEmail from "@/emails/billing-trial-ending";
import BillingPaymentFailedEmail from "@/emails/billing-payment-failed";
import BillingPauseWarningEmail from "@/emails/billing-pause-warning";
import BillingPaymentConfirmedEmail from "@/emails/billing-payment-confirmed";

export type BillingEmailTemplate = "trial-ending" | "payment-failed" | "pause-warning" | "payment-confirmed";

export async function sendBillingEmail(args: {
  template: BillingEmailTemplate;
  to: string;
  orgName: string;
  data: Record<string, unknown>;
}): Promise<void> {
  const subject = subjectFor(args.template, args.orgName);
  const react = componentFor(args.template, args.orgName, args.data);
  await resend.emails.send({
    from: `Tus Horarios <${FROM_NOTIF}>`,
    to: args.to,
    subject,
    react,
  });
}

function subjectFor(t: BillingEmailTemplate, org: string): string {
  switch (t) {
    case "trial-ending": return `Tu trial de Tus Horarios vence pronto`;
    case "payment-failed": return `No pudimos cobrar tu suscripción`;
    case "pause-warning": return `Tu suscripción se pausará en 2 días`;
    case "payment-confirmed": return `Pago confirmado — Tus Horarios`;
  }
}

function componentFor(t: BillingEmailTemplate, orgName: string, data: any) {
  const baseProps = { orgName, ...data };
  switch (t) {
    case "trial-ending": return BillingTrialEndingEmail(baseProps);
    case "payment-failed": return BillingPaymentFailedEmail(baseProps);
    case "pause-warning": return BillingPauseWarningEmail(baseProps);
    case "payment-confirmed": return BillingPaymentConfirmedEmail(baseProps);
  }
}
```

- [ ] **Step 5: Reemplazar stub en webhook handler** (importar `sendBillingEmail`).

- [ ] **Step 6: Build verde**

- [ ] **Step 7: Commit**

```bash
git add supabase/migrations/048_sent_reminders.sql supabase/tests/ src/emails/billing-*.tsx src/lib/emails/send-billing-emails.ts src/app/api/webhooks/wompi/route.ts src/lib/supabase/database.types.ts
git commit -m "feat(billing): 4 email templates + helper + sent_reminders dedup"
```

### Task 19: Cron `/api/cron/billing/process-cycles`

**Files:**
- Create: `src/app/api/cron/billing/process-cycles/route.ts`
- Create: `vercel.json`

- [ ] **Step 1: Crear vercel.json con crons**

```json
{
  "crons": [
    { "path": "/api/cron/billing/process-cycles", "schedule": "0 8 * * *" },
    { "path": "/api/cron/billing/reminders", "schedule": "0 14 * * *" }
  ]
}
```

(Schedules UTC: 03:00 COT y 09:00 COT respectivamente)

- [ ] **Step 2: Cron endpoint con auth + lógica**

```typescript
// src/app/api/cron/billing/process-cycles/route.ts
import { NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/admin";
import { createTransaction } from "@/lib/billing/wompi/client";
import { calculateNextPeriodEnd, copToCents, calculateIva, calculateTotalWithIva } from "@/lib/billing/engine";

export const runtime = "nodejs";
export const maxDuration = 300;     // 5 min fluid

export async function GET(req: Request) {
  const auth = req.headers.get("authorization");
  if (auth !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: "unauthorized" }, { status: 401 });
  }

  const supabase = createAdminClient();
  const now = new Date();

  const { data: subs } = await supabase
    .from("subscriptions")
    .select("*, organizations!inner(billing_exempt, billing_email, name)")
    .lte("current_period_end", now.toISOString())
    .in("status", ["active", "trialing"])
    .eq("organizations.billing_exempt", false);

  const results = { processed: 0, charged: 0, failed: 0, paused: 0 };

  for (const sub of subs ?? []) {
    results.processed++;

    if (!sub.payment_method_id) {
      // sin tarjeta: trial venció → past_due
      await supabase.from("subscriptions").update({ status: "past_due" }).eq("id", sub.id);
      results.failed++;
      continue;
    }

    const { data: plan } = await supabase.from("plans").select("*").eq("id", sub.plan_id).single();
    const { data: pm } = await supabase.from("payment_methods").select("*").eq("id", sub.payment_method_id).single();

    const amount = plan!.price_cop;
    const iva = calculateIva(amount);
    const total = calculateTotalWithIva(amount);

    const { data: invoice } = await supabase.from("invoices").insert({
      organization_id: sub.organization_id,
      subscription_id: sub.id,
      plan_id: sub.plan_id,
      period_start: now.toISOString(),
      period_end: calculateNextPeriodEnd(now).toISOString(),
      amount_cop: amount, iva_cop: iva, total_cop: total,
      status: "open",
      due_date: now.toISOString(),
    }).select("id").single();

    try {
      const tx = await createTransaction({
        paymentSourceId: pm!.provider_payment_source_id,
        amountInCents: copToCents(total),
        currency: "COP",
        reference: invoice!.id,
        customerEmail: (sub as any).organizations.billing_email ?? "billing@tushorarios.com",
        recurrent: true,
      });

      await supabase.from("payments").insert({
        invoice_id: invoice!.id,
        payment_method_id: pm!.id,
        provider: "wompi",
        provider_transaction_id: tx.id,
        amount_cop: total,
        status: tx.status === "APPROVED" ? "approved" : tx.status === "DECLINED" ? "declined" : "pending",
      });

      if (tx.status === "APPROVED") {
        // webhook handler hará el rest, pero por defensa actualizamos invoice si llega APPROVED síncrono
        results.charged++;
      }
    } catch (err: any) {
      console.error("[cron process-cycles] charge failed", err);
      await supabase.from("invoices").update({ status: "failed" }).eq("id", invoice!.id);
      await supabase.from("subscriptions").update({ status: "past_due" }).eq("id", sub.id);
      results.failed++;
    }
  }

  return NextResponse.json(results);
}
```

- [ ] **Step 3: Test manual sandbox**

```bash
curl -H "Authorization: Bearer $CRON_SECRET" https://app-horarios-XXX.vercel.app/api/cron/billing/process-cycles
```

- [ ] **Step 4: Commit**

```bash
git add src/app/api/cron/billing/process-cycles/route.ts vercel.json
git commit -m "feat(cron): process-cycles diario + vercel.json schedule"
```

### Task 20: Cron `/api/cron/billing/reminders` + DIAN job processor cron

**Files:**
- Create: `src/app/api/cron/billing/reminders/route.ts`
- Modify: `vercel.json` (3er cron para DIAN job processor)

- [ ] **Step 1: Reminders endpoint con dunning engine + dedup**

```typescript
// src/app/api/cron/billing/reminders/route.ts
import { NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/admin";
import { decideDunningAction } from "@/lib/billing/dunning";
import { sendBillingEmail } from "@/lib/emails/send-billing-emails";
import { processDianEmitJobs } from "@/lib/billing/dian-emit-job";

export const runtime = "nodejs";
export const maxDuration = 300;

export async function GET(req: Request) {
  const auth = req.headers.get("authorization");
  if (auth !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: "unauthorized" }, { status: 401 });
  }

  const supabase = createAdminClient();
  const now = new Date();

  // 1. Reminders
  const { data: subs } = await supabase
    .from("subscriptions")
    .select("*, organizations!inner(name, billing_email, billing_exempt)")
    .in("status", ["trialing", "past_due"])
    .eq("organizations.billing_exempt", false);

  const emailsSent: string[] = [];
  const transitions: Record<string, number> = {};

  for (const sub of subs ?? []) {
    const action = decideDunningAction(sub as any, now);
    if (!action) continue;

    if (action.kind === "transition") {
      await supabase.from("subscriptions").update({ status: action.to }).eq("id", sub.id);
      if (action.to === "paused") {
        await supabase.from("organizations").update({ status: "paused" }).eq("id", sub.organization_id);
      }
      transitions[action.to] = (transitions[action.to] ?? 0) + 1;
    } else {
      // Dedup
      const { data: alreadySent } = await supabase
        .from("sent_reminders")
        .select("id")
        .eq("organization_id", sub.organization_id)
        .eq("template", action.template)
        .eq("days_offset", action.daysOffset)
        .maybeSingle();
      if (alreadySent) continue;

      const org = (sub as any).organizations;
      try {
        await sendBillingEmail({
          template: action.template,
          to: org.billing_email ?? "billing@tushorarios.com",
          orgName: org.name,
          data: { daysOffset: action.daysOffset, planId: sub.plan_id },
        });
        await supabase.from("sent_reminders").insert({
          organization_id: sub.organization_id,
          template: action.template,
          days_offset: action.daysOffset,
        });
        emailsSent.push(`${org.name}/${action.template}`);
      } catch (err) {
        console.error("[reminders] send failed", err);
      }
    }
  }

  // 2. DIAN async job processing en mismo cron (no requiere cron separado para queue chico)
  const dianResult = await processDianEmitJobs();

  return NextResponse.json({ emailsSent, transitions, dianResult });
}
```

- [ ] **Step 2: Build + lint**

- [ ] **Step 3: Commit**

```bash
git add src/app/api/cron/billing/reminders/route.ts
git commit -m "feat(cron): reminders engine + DIAN job processor (2 cron en uno)"
```

---

## Phase 7 — Hooks + UI components

### Task 21: Hooks `useBillingStatus` + `useBillingInvoices`

**Files:**
- Create: `src/hooks/use-billing-status.ts`
- Create: `src/hooks/use-billing-invoices.ts`

- [ ] **Step 1: useBillingStatus (singleton query)**

```typescript
// src/hooks/use-billing-status.ts
"use client";
import { useEffect, useState } from "react";
import { createClient } from "@/lib/supabase/client";

type BillingStatus = {
  subscription: { status: string; current_period_end: string; cancel_at_period_end: boolean } | null;
  trialDaysLeft: number | null;
  isPaused: boolean;
  isPastDue: boolean;
  isTrialingNearEnd: boolean;
};

const POLL_MS = 60_000;

export function useBillingStatus(enabled: boolean): BillingStatus {
  const [state, setState] = useState<BillingStatus>({
    subscription: null, trialDaysLeft: null, isPaused: false, isPastDue: false, isTrialingNearEnd: false
  });

  useEffect(() => {
    if (!enabled) return;
    const supabase = createClient();
    let cancelled = false;

    const fetchOnce = async () => {
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) return;
      const { data: profile } = await supabase.from("profiles").select("organization_id").eq("id", user.id).maybeSingle();
      if (!profile?.organization_id) return;
      const { data: sub } = await supabase
        .from("subscriptions")
        .select("status, current_period_end, cancel_at_period_end")
        .eq("organization_id", profile.organization_id)
        .maybeSingle();
      if (cancelled) return;
      if (!sub) {
        setState({ subscription: null, trialDaysLeft: null, isPaused: false, isPastDue: false, isTrialingNearEnd: false });
        return;
      }
      const daysLeft = Math.floor((new Date(sub.current_period_end).getTime() - Date.now()) / (24 * 60 * 60 * 1000));
      setState({
        subscription: sub,
        trialDaysLeft: sub.status === "trialing" ? daysLeft : null,
        isPaused: sub.status === "paused",
        isPastDue: sub.status === "past_due",
        isTrialingNearEnd: sub.status === "trialing" && daysLeft <= 7,
      });
    };

    fetchOnce();
    const id = setInterval(fetchOnce, POLL_MS);
    const onFocus = () => fetchOnce();
    window.addEventListener("focus", onFocus);
    return () => { cancelled = true; clearInterval(id); window.removeEventListener("focus", onFocus); };
  }, [enabled]);

  return state;
}
```

- [ ] **Step 2: useBillingInvoices**

```typescript
// src/hooks/use-billing-invoices.ts
"use client";
import { useEffect, useState } from "react";
import { createClient } from "@/lib/supabase/client";
import type { Invoice } from "@/lib/billing/types";

export function useBillingInvoices(enabled: boolean): { invoices: Invoice[]; loading: boolean } {
  const [invoices, setInvoices] = useState<Invoice[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    if (!enabled) { setLoading(false); return; }
    const supabase = createClient();
    let cancelled = false;
    const load = async () => {
      const { data: { user } } = await supabase.auth.getUser();
      if (!user) return;
      const { data: profile } = await supabase.from("profiles").select("organization_id").eq("id", user.id).maybeSingle();
      if (!profile?.organization_id) return;
      const { data } = await supabase
        .from("invoices")
        .select("*")
        .eq("organization_id", profile.organization_id)
        .order("created_at", { ascending: false })
        .limit(100);
      if (!cancelled) {
        setInvoices(data as any ?? []);
        setLoading(false);
      }
    };
    load();
    return () => { cancelled = true; };
  }, [enabled]);

  return { invoices, loading };
}
```

- [ ] **Step 3: Commit**

```bash
git add src/hooks/use-billing-*.ts
git commit -m "feat(hooks): useBillingStatus + useBillingInvoices"
```

### Task 22: UI components batch (parallelizable internamente — pero 1 subagent puede hacerlos todos)

**Files:**
- Create: `src/components/billing/billing-banner.tsx`
- Create: `src/components/billing/plan-card.tsx`
- Create: `src/components/billing/wompi-widget-button.tsx`
- Create: `src/components/billing/payment-method-card.tsx`
- Create: `src/components/billing/dian-provider-config.tsx`
- Create: `src/components/billing/invoice-history-table.tsx`

**Antes de empezar:** invocar `modern-web-guidance:modern-web-guidance` con query `"form label aria-invalid"` y `"dialog popover native"` para aplicar patrones a11y/CWV correctos.

- [ ] **Step 1: BillingBanner** (color amber/red según status, mount en layout)

```tsx
// src/components/billing/billing-banner.tsx
"use client";
import Link from "next/link";
import { AlertTriangle } from "lucide-react";
import { useBillingStatus } from "@/hooks/use-billing-status";
import { useAuth } from "@/hooks/use-auth";
import { canAdmin } from "@/lib/auth/can-manage";

export function BillingBanner() {
  const { profile } = useAuth();
  const enabled = canAdmin(profile?.role);
  const { isTrialingNearEnd, isPastDue, trialDaysLeft } = useBillingStatus(enabled);

  if (!enabled) return null;
  if (!isTrialingNearEnd && !isPastDue) return null;

  const color = isPastDue ? "bg-red-50 border-red-200 text-red-900" : "bg-amber-50 border-amber-200 text-amber-900";
  const message = isPastDue
    ? "No pudimos cobrar tu última factura. Actualiza tu tarjeta para evitar la pausa de tu cuenta."
    : `Tu trial vence en ${trialDaysLeft} día${trialDaysLeft === 1 ? "" : "s"}. Agrega un método de pago.`;

  return (
    <div role="alert" className={`flex items-center gap-3 border-l-4 px-4 py-3 ${color}`}>
      <AlertTriangle className="h-5 w-5 flex-shrink-0" aria-hidden />
      <p className="flex-1 text-sm">{message}</p>
      <Link href="/facturacion" className="text-sm font-semibold underline">
        {isPastDue ? "Actualizar tarjeta" : "Agregar tarjeta"}
      </Link>
    </div>
  );
}
```

- [ ] **Step 2: PlanCard** (1 card por plan, render dentro del plan picker dialog)

```tsx
// src/components/billing/plan-card.tsx
"use client";
import { Button } from "@/components/ui/button";
import { Check } from "lucide-react";
import type { Plan } from "@/lib/billing/types";

export function PlanCard({ plan, isCurrent, onChoose }: { plan: Plan; isCurrent: boolean; onChoose: () => void }) {
  const cop = new Intl.NumberFormat("es-CO", { style: "currency", currency: "COP", maximumFractionDigits: 0 });
  return (
    <article className={`rounded-lg border p-6 ${isCurrent ? "border-primary bg-accent/30" : "border-border"}`}>
      <h3 className="text-xl font-bold">{plan.name}</h3>
      <p className="mt-2 text-3xl font-bold">{cop.format(plan.price_cop)}<span className="text-base font-normal text-muted-foreground">/mes</span></p>
      <ul className="mt-4 space-y-2 text-sm">
        <li className="flex items-center gap-2"><Check className="h-4 w-4 text-green-600" aria-hidden />Hasta {plan.max_employees ?? "ilimitados"} empleados</li>
      </ul>
      {isCurrent ? (
        <Button disabled className="mt-6 w-full">Tu plan actual</Button>
      ) : plan.contact_sales ? (
        <Button variant="outline" className="mt-6 w-full" asChild>
          <a href="mailto:hola@tushorarios.com?subject=Plan Enterprise">Contactar ventas</a>
        </Button>
      ) : (
        <Button onClick={onChoose} className="mt-6 w-full">Elegir {plan.name}</Button>
      )}
    </article>
  );
}
```

- [ ] **Step 3: WompiWidgetButton** (carga script Wompi widget + maneja respuesta)

```tsx
// src/components/billing/wompi-widget-button.tsx
"use client";
import Script from "next/script";
import { useCallback, useState } from "react";
import { Button } from "@/components/ui/button";

declare global {
  interface Window { WidgetCheckout?: any }
}

export function WompiWidgetButton({ planId, label = "Pagar y suscribirse" }: { planId: string; label?: string }) {
  const [loading, setLoading] = useState(false);

  const onClick = useCallback(async () => {
    setLoading(true);
    try {
      const res = await fetch("/api/billing/wompi/prepare-checkout", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ planId }),
      });
      if (!res.ok) {
        const err = await res.json();
        alert(err.error ?? "Error preparando pago");
        return;
      }
      const cfg = await res.json();
      if (!window.WidgetCheckout) {
        alert("Widget Wompi no cargó. Recarga la página.");
        return;
      }
      const checkout = new window.WidgetCheckout({
        currency: cfg.currency,
        amountInCents: cfg.amountInCents,
        reference: cfg.reference,
        publicKey: cfg.publicKey,
        signature: { integrity: cfg.signature },
        redirectUrl: cfg.redirectUrl,
        customerData: { email: cfg.customerEmail },
      });
      checkout.open((result: any) => { console.log("[wompi widget result]", result); });
    } finally {
      setLoading(false);
    }
  }, [planId]);

  return (
    <>
      <Script src="https://checkout.wompi.co/widget.js" strategy="afterInteractive" />
      <Button onClick={onClick} disabled={loading}>{loading ? "Preparando…" : label}</Button>
    </>
  );
}
```

- [ ] **Step 4: PaymentMethodCard, DianProviderConfig, InvoiceHistoryTable**

(Patrones similares — el subagent los implementa siguiendo shadcn conventions, useBillingStatus/useBillingInvoices hooks, DataTable<T> shared component.)

- [ ] **Step 5: Build verde**

- [ ] **Step 6: Commit**

```bash
git add src/components/billing/
git commit -m "feat(billing): 6 UI components (banner, plan card, widget btn, pm card, dian config, invoice table)"
```

### Task 23: Page `/facturacion` + sidebar item + layout banner mount

**Files:**
- Create: `src/app/(authenticated)/facturacion/page.tsx`
- Modify: `src/components/layout/sidebar.tsx`
- Modify: `src/app/(authenticated)/layout.tsx`

- [ ] **Step 1: Page con 4 tabs (Tabs de shadcn)**

```tsx
// src/app/(authenticated)/facturacion/page.tsx
"use client";
import { useEffect, useState } from "react";
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
import { useAuth } from "@/hooks/use-auth";
import { canAdmin } from "@/lib/auth/can-manage";
import { useBillingStatus } from "@/hooks/use-billing-status";
// + imports de componentes Task 22 + Plan, fetch /api/billing/...

export default function FacturacionPage() {
  const { profile } = useAuth();
  if (!canAdmin(profile?.role)) {
    return <p className="text-muted-foreground">No tienes permisos para ver esta página.</p>;
  }
  return (
    <div className="space-y-6">
      <header>
        <h1 className="text-3xl font-bold">Facturación</h1>
        <p className="text-muted-foreground">Gestiona tu plan, método de pago y facturas.</p>
      </header>
      <Tabs defaultValue="plan">
        <TabsList>
          <TabsTrigger value="plan">Plan</TabsTrigger>
          <TabsTrigger value="payment">Método de pago</TabsTrigger>
          <TabsTrigger value="dian">DIAN</TabsTrigger>
          <TabsTrigger value="history">Historial</TabsTrigger>
        </TabsList>
        <TabsContent value="plan">{/* Plan tab content */}</TabsContent>
        <TabsContent value="payment">{/* PaymentMethodCard + WompiWidgetButton */}</TabsContent>
        <TabsContent value="dian">{/* DianProviderConfig */}</TabsContent>
        <TabsContent value="history">{/* InvoiceHistoryTable */}</TabsContent>
      </Tabs>
    </div>
  );
}
```

- [ ] **Step 2: Sidebar — agregar Facturación + badge**

```tsx
// src/components/layout/sidebar.tsx — agregar a topNavigation:
{ name: "Facturación", href: "/facturacion", icon: CreditCard, roles: ["super_admin", "admin"] },

// + import useBillingStatus
// + badge rojo si isPastDue, paralelo a renderBadge ya existente
```

- [ ] **Step 3: Layout mount BillingBanner**

```tsx
// src/app/(authenticated)/layout.tsx — agregar arriba del main content:
import { BillingBanner } from "@/components/billing/billing-banner";
// ...
<main>
  <BillingBanner />
  {children}
</main>
```

- [ ] **Step 4: Build verde**

- [ ] **Step 5: Commit**

```bash
git add src/app/\(authenticated\)/facturacion/page.tsx src/components/layout/sidebar.tsx src/app/\(authenticated\)/layout.tsx
git commit -m "feat(billing): /facturacion page + sidebar item + layout banner mount"
```

### Task 24: Middleware R10 paused-block

**Files:**
- Modify: `src/lib/supabase/middleware.ts`

- [ ] **Step 1: Extender query profile con subscription**

```typescript
// dentro de updateSession, después del fetch de profile:
const { data: subData } = await supabase
  .from("subscriptions")
  .select("status")
  .eq("organization_id", profile?.organization_id ?? "")
  .maybeSingle();
const subscriptionStatus = subData?.status ?? null;

// R10: paused block
if (
  user &&
  profile?.organization_id &&
  subscriptionStatus === "paused" &&
  !path.startsWith("/facturacion") &&
  !path.startsWith("/api/") &&
  !pathIsPublic
) {
  const url = request.nextUrl.clone();
  url.pathname = "/facturacion";
  return NextResponse.redirect(url);
}
```

- [ ] **Step 2: Test manual:** crear sub con status=paused via SQL → verificar redirect

- [ ] **Step 3: Commit**

```bash
git add src/lib/supabase/middleware.ts
git commit -m "feat(middleware): R10 paused org → /facturacion only"
```

---

## Phase 8 — Integration + smoke

### Task 25: Integration tests con Wompi sandbox

**Files:**
- Create: `src/lib/billing/wompi/__tests__/integration.test.ts`

- [ ] **Step 1: Tests guarded por env vars**

```typescript
import { describe, it, expect, beforeAll } from "vitest";
import { getAcceptanceTokens, createTransaction } from "../client";

const SANDBOX_AVAILABLE = !!process.env.WOMPI_PRIVATE_KEY && process.env.WOMPI_PRIVATE_KEY.startsWith("prv_test_");

describe.skipIf(!SANDBOX_AVAILABLE)("Wompi sandbox integration", () => {
  it("getAcceptanceTokens devuelve 2 tokens válidos", async () => {
    const tokens = await getAcceptanceTokens();
    expect(tokens.acceptance_token).toBeTruthy();
    expect(tokens.accept_personal_auth).toBeTruthy();
  });

  // Más tests cuando user tenga payment_source de prueba creado
});
```

- [ ] **Step 2: Documentar en CLAUDE.md / README** que para correr esto necesitan keys sandbox.

- [ ] **Step 3: Commit**

```bash
git add src/lib/billing/wompi/__tests__/
git commit -m "test(wompi): integration tests guarded por sandbox env"
```

### Task 26: E2E smoke browser (sandbox)

**Files:**
- Sin archivos nuevos (smoke manual con chrome-devtools-mcp)

- [ ] **Step 1: Setup test data en Supabase Cloud**

Crear org test temporal `smoke-billing` + admin via service_role (patrón del smoke sub-proy 5).

- [ ] **Step 2: Browser flow:**
  1. Login como admin smoke-billing en `smoke-billing.tushorarios.com`
  2. /facturacion → click "Pro" → "Pagar y suscribirse"
  3. Wompi widget abre → tarjeta test `4242 4242 4242 4242` → submit
  4. Verificar invoice paid + subscription active + dashboard sin banner
  5. Test past_due con tarjeta `4242 4242 4242 0002` → banner aparece
  6. Test cancel → cancel_at_period_end=true

- [ ] **Step 3: Cleanup test data**

- [ ] **Step 4: Commit memoria smoke (en .claude/projects/...)** con resultados.

---

## Phase 9 — Deploy con feature flag

### Task 27: Feature flag `BILLING_ENABLED` + deploy gating

**Files:**
- Modify: `src/components/layout/sidebar.tsx` (hide Facturación si flag off)
- Modify: `src/app/(authenticated)/facturacion/page.tsx` (return 404 si flag off)
- Modify: `src/components/billing/billing-banner.tsx` (return null si flag off)

- [ ] **Step 1: Helper `isBillingEnabled()`**

```typescript
// src/lib/billing/feature-flag.ts
export function isBillingEnabled(): boolean {
  return process.env.NEXT_PUBLIC_BILLING_ENABLED === "true";
}
```

- [ ] **Step 2: Aplicar gate** en sidebar, page, banner.

- [ ] **Step 3: Env var setup**

User setea `NEXT_PUBLIC_BILLING_ENABLED=false` en Vercel inicialmente (todos los 3 environments). Yo via REST API en sesión (patrón Task 13 sub-proy 5):

- [ ] **Step 4: Deploy commit**

```bash
git add src/lib/billing/feature-flag.ts src/components/billing/billing-banner.tsx src/components/layout/sidebar.tsx src/app/\(authenticated\)/facturacion/page.tsx
git commit -m "feat(billing): feature flag BILLING_ENABLED para soft launch"
git push origin main
```

- [ ] **Step 5: Verificar deploy Vercel ready + sub-proy 6 completo sin afectar usuarios**

- [ ] **Step 6: Flip flag a `true` solo para LR (manual test) → smoke → general availability**

---

## Self-review

(Realizado por mí como autor del plan — gaps identificados o pendientes anotados acá)

**Spec coverage:**
- ✓ Sección 2 D1-D12: todas las decisiones tienen task asignada
- ✓ Schema sección 3: Task 1 + 17 + 18 (migraciones 046, 047, 048)
- ✓ Wompi sección 4: Tasks 6, 7, 8, 12, 16
- ✓ Subscription lifecycle sección 5: Tasks 3, 4, 19, 20
- ✓ DIAN sección 6: Tasks 9, 10, 11, 17
- ✓ UI sección 7: Tasks 21, 22, 23, 24
- ✓ Edge cases sección 8: covered en webhook (idempotencia), cron (billing_exempt), middleware (paused)
- ✓ Testing sección 9: Tasks de vitest + SQL + integration + E2E
- ✓ Rollout sección 10: Tasks ordenados + Task 27 feature flag

**Type consistency:**
- `Plan` definido en Task 2, usado en Tasks 3, 11, 12, 14, 22
- `Subscription` definido en Task 2, usado en Tasks 4, 14, 16, 19, 20, 21
- `BillingProvider` interface en Task 9, implementado en 10 + 11, usado en 17
- `WompiWebhookPayload` en Task 7, usado en Task 16
- `DunningAction` en Task 4, usado en Task 20

**Placeholders / gaps:**
- Hash exacto del vector Wompi (Task 6 Step 3) requiere validar contra openssl real. Spec en lugar de hardcoded sirve como guía.
- Webhook verify signature algorithm (Task 7 Step 2) tiene comment "verificar contra doc oficial actual". Confirmar HMAC vs SHA256 directo durante implementación — el código tiene ambos paths.
- Email templates Task 18 Step 3 dice "el subagent escribe los 4 siguiendo el styling existente". Plantilla referenciada es `src/emails/demo-request-confirmation.tsx` para que el subagent copie estructura.
- Componentes Task 22 Step 4 (PaymentMethodCard, DianProviderConfig, InvoiceHistoryTable) listados sin código completo — patrón es shadcn/ui form + DataTable existente. Subagent debe seguir el patrón `RestRuleCards` o similar.

**Tareas que el subagent puede paralelizar (mismo mensaje):**
- T3-T5 (engine, dunning, crypto) — todas pure logic con tests, sin DB deps
- T6-T7 (integrity-hash, webhook-verify) — pure
- T9-T11 (providers interface, manual, alegra) — independientes después de T2
- T13-T15 (API endpoints simples) — independientes después de T12

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-24-billing.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — Dispatcher subagents per task, review entre tasks, fast iteration

**2. Inline Execution** — Tasks en esta misma sesión via executing-plans, batch execution con checkpoints

**¿Cuál prefieres?**
